export const VendorServerNote = () => (
  <Note>
    **Vendor** é uma conexão de saída para quem transporta seu tráfego. **Server** é um listener no
    qual seus clientes se conectam. A configuração os chama de `smppclient` e `smppserver`; este site
    e o painel de controle nunca fazem isso. Consulte [Conceitos](/platform/concepts).
  </Note>
);

export const RefusalNote = () => (
  <Warning>
    O FireFlo recusa em vez de adivinhar em mais lugares do que a maioria dos gateways. Onde uma
    página diz que algo é recusado, isso é deliberado e a recusa é o comportamento documentado. Não é
    uma limitação esperando para ser removida.
  </Warning>
);
